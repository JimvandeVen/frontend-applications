module.exports = calculator


function calculator (answer) {
  console.log('calculate', answer)
  var sum = answer
  return sum
}

// Number( ( 1 / ( 1 + Math.exp( -1 * ( -8.57219 + X ) ) ) * 100 ).toFixed( Y ) )
